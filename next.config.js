/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['www.orb.ac', 'upload.wikimedia.org', 'ik.imagekit.io', 'ipfs.io'],
    },
  }
  
  module.exports = nextConfig
  